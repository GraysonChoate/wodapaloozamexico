# Raw client media — 1.4 GB. Lives in Drive, never in git.
assets/video/**/*.mov
assets/video/**/*.mp4
assets/images/**/*.jpg
Motion Site Screen recordings/

# Reference downloads
reference/*.mp4

.DS_Store
WPL MEXICO VIDEOS/

# real-browser verification screenshots — regenerate with build/verify-browser.sh
build/.verify-shots/

# Raw b-roll dropped into the repo — stays local, same rule as assets/video
Other B-Roll/
